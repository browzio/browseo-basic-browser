<UserControl x:Class="Organiser.Common.Controlls.TrvMacrosUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Organiser.Common.Controlls"
             xmlns:commonConvs="clr-namespace:Organiser.Common.Converters"
             xmlns:commonClass="clr-namespace:Organiser.Common.Classes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Organiser.Common;component/GloableStyleDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <SolidColorBrush Color="#5c5c5c" x:Key="text_fg"/>
            <SolidColorBrush Color="#f5f5f5" x:Key="bg_gray"/>
            <SolidColorBrush Color="#eeeeee" x:Key="gray_bg"/>
            <Style x:Key="TreeViewItemFocusVisual">
                <Setter Property="Control.Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Rectangle/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <PathGeometry x:Key="TreeArrow" Figures="M0,0 L0,6 L6,0 z"/>
            <Style x:Key="ExpandCollapseToggleStyle" TargetType="{x:Type ToggleButton}">
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="Width" Value="16"/>
                <Setter Property="Height" Value="16"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                            <Border Background="Transparent" Height="16" Padding="5,5,5,5" Width="16">
                                <Path x:Name="ExpandPath" Data="{StaticResource TreeArrow}" Fill="Transparent" Stroke="#FF989898">
                                    <Path.RenderTransform>
                                        <RotateTransform Angle="135" CenterY="3" CenterX="3"/>
                                    </Path.RenderTransform>
                                </Path>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Stroke" TargetName="ExpandPath" Value="#FF1BBBFA"/>
                                    <Setter Property="Fill" TargetName="ExpandPath" Value="Transparent"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="RenderTransform" TargetName="ExpandPath">
                                        <Setter.Value>
                                            <RotateTransform Angle="180" CenterY="3" CenterX="3"/>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Fill" TargetName="ExpandPath" Value="#FF595959"/>
                                    <Setter Property="Stroke" TargetName="ExpandPath" Value="#FF262626"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <commonConvs:OpisiteVisibilityConverter x:Key="convOppositVisibility" />
            <commonConvs:BooleanToVisible x:Key="convBoolYesNotVisibility" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <TreeView ItemsSource="{Binding MacroFilesBase}" Name="trvMacros" AllowDrop="True"
                                  BorderThickness="0" Background="White" VerticalAlignment="Stretch">
            <TreeView.ItemContainerStyle>
                <!-- This Style binds a TreeViewItem to a TreeViewItemViewModel. -->
                <Style TargetType="{x:Type TreeViewItem}">
                    <EventSetter Event="TreeViewItem.DragOver" Handler="treeView_DragOver"/>
                    <EventSetter Event="TreeViewItem.Drop" Handler="treeView_Drop"/>
                    <EventSetter Event="TreeViewItem.MouseMove" Handler="treeView_MouseMove"/>

                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TreeViewItem}">
                                <Grid >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition MinWidth="0" Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <ToggleButton x:Name="Expander" ClickMode="Press" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" Style="{StaticResource ExpandCollapseToggleStyle}"/>
                                    <Border x:Name="Bd" Height="25" BorderThickness="0" Background="Transparent" Grid.Column="1" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true" Margin="0,0,8,0">
                                        <ContentPresenter x:Name="PART_Header" 
                                                                      ContentSource="Header"
                                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                                                      VerticalAlignment="Center"
                                                                      TextElement.Foreground="{StaticResource text_fg}" 
                                                                      TextElement.FontSize="12" 
                                                                      Margin="10,0" />
                                    </Border>
                                    <ItemsPresenter x:Name="ItemsHost" Grid.ColumnSpan="2" Grid.Column="1" Grid.Row="1"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsExpanded" Value="false">
                                        <Setter Property="Visibility" TargetName="ItemsHost" Value="Collapsed"/>
                                    </Trigger>
                                    <Trigger Property="HasItems" Value="false">
                                        <Setter Property="Visibility" TargetName="Expander" Value="Hidden"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="true">
                                        <Setter Property="Background" TargetName="Bd" Value="{StaticResource gray_bg}"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="true"/>
                                            <Condition Property="IsSelectionActive" Value="false"/>
                                        </MultiTrigger.Conditions>
                                        <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                                    </MultiTrigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="false"/>
                                            <Condition Property="commonClass:MyTreeViewHelper.IsMouseDirectlyOverItem" Value="True" />
                                        </MultiTrigger.Conditions>
                                        <MultiTrigger.Setters>
                                            <Setter Property="Background" TargetName="Bd" Value="#FFFBFBFB" />
                                            <!--<Setter Property="Cursor" Value="Hand" />-->
                                        </MultiTrigger.Setters>
                                    </MultiTrigger>
                                    <Trigger Property="IsEnabled" Value="false">
                                        <Setter Property="Opacity" Value="0.4" TargetName="Bd"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TreeView.ItemContainerStyle>

            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type commonClass:MacroFile}" ItemsSource="{Binding NextMacros}">
                    <StackPanel Name="spMacros" Orientation="Horizontal" PreviewMouseDown="spMacros_PreviewMouseDown" PreviewMouseRightButtonDown="spMacros_PreviewMouseRightButtonDown">

                        <CheckBox IsChecked="{Binding IsMacroChecked}">
                            <CheckBox.Style>
                                <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource {x:Type CheckBox}}">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Path=IsFolder}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>
                        
                        <Image Margin="1" Width="10" Height="10" Stretch="Uniform">
                            <Image.Style>
                                <Style TargetType="{x:Type Image}">
                                    <Setter Property="Source" Value="pack://application:,,,/Organiser.Common;component/Image/new_document.png" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Path=IsFolder}" Value="True">
                                            <Setter Property="Source" Value="pack://application:,,,/Organiser.Common;component/Image/closed_folder.png"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                        <TextBlock Text="{Binding FileName}" MinWidth="145" MaxWidth="300" VerticalAlignment="Center">
                            <TextBlock.ToolTip>
                                <StackPanel>
                                    <Image Source="{Binding TooltipType}" />
                                    <TextBlock Text="{Binding TooltipText}" FontSize="12" Margin="8,-105,0,0" />
                                </StackPanel>
                                <!--<Image>
                                    <Image.Style>
                                        <Style TargetType="{x:Type Image}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Path=TooltipType}" Value="KeyWords">
                                                    <Setter Property="Source" Value="pack://application:,,,/Organiser.Common;component/Image/tooltipKeywords.png"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Path=TooltipType}" Value="iftt">
                                                    <Setter Property="Source" Value="pack://application:,,,/Organiser.Common;component/Image/tooltipiftt.png"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Path=TooltipType}" Value="ytVidUpload">
                                                    <Setter Property="Source" Value="pack://application:,,,/Organiser.Common;component/Image/tooltipvideoUpload.png"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Path=TooltipType}" Value="fbProfPicUpload">
                                                    <Setter Property="Source" Value="pack://application:,,,/Organiser.Common;component/Image/tooltipfbProfilephoto.png"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Path=TooltipType}" Value="fbProfPicUpload">
                                                    <Setter Property="Source" Value="pack://application:,,,/Organiser.Common;component/Image/tooltipCreateBoard.png"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Path=TooltipType}" Value="tooltippintrestLikeReshare">
                                                    <Setter Property="Source" Value="pack://application:,,,/Organiser.Common;component/Image/tooltipCreateBoard.png"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>-->
                            </TextBlock.ToolTip>
                        </TextBlock>

                        <StackPanel.ContextMenu>
                            <ContextMenu Opened="ContextMenu_Opened" Closed="ContextMenu_Closed">
                                <!--#region folders-->
                                <MenuItem Header="New Folder" 
                                                          Visibility="{Binding Path=IsFolder, Converter={StaticResource convBoolYesNotVisibility}, ConverterParameter=YES}"
                                                          Command="{Binding OnCommandFromView}" CommandParameter="FolderNew" />
                                <MenuItem Header="Add Macros" 
                                                          Visibility="{Binding Path=IsFolder, Converter={StaticResource convBoolYesNotVisibility}, ConverterParameter=YES}"
                                                          Command="{Binding OnCommandFromView}" CommandParameter="FolderAddMacros" />
                                <MenuItem Header="Rename" 
                                                          Visibility="{Binding Path=IsFolder, Converter={StaticResource convBoolYesNotVisibility}, ConverterParameter=YES}"
                                                          Command="{Binding OnCommandFromView}" CommandParameter="FolderRename" />
                                <MenuItem Header="Delete" 
                                                          Visibility="{Binding Path=IsFolder, Converter={StaticResource convBoolYesNotVisibility}, ConverterParameter=YES}"
                                                          Command="{Binding OnCommandFromView}" CommandParameter="FolderDelete" />
                                <!--#endregion-->

                                <!--#region macros-->
                                <MenuItem Header="Quick Run" 
                                                          Visibility="{Binding Path=IsFolder, Converter={StaticResource convBoolYesNotVisibility}, ConverterParameter=NOT}"
                                                          Command="{Binding OnCommandFromView}" CommandParameter="MacroRun" />
                                <MenuItem Header="Edit" 
                                                          Visibility="{Binding Path=IsFolder, Converter={StaticResource convBoolYesNotVisibility}, ConverterParameter=NOT}"
                                                          Command="{Binding OnCommandFromView}" CommandParameter="MacroEdit" />
                                <MenuItem Header="Rename" 
                                                          Visibility="{Binding Path=IsFolder, Converter={StaticResource convBoolYesNotVisibility}, ConverterParameter=NOT}"
                                                          Command="{Binding OnCommandFromView}" CommandParameter="MacroRename" />
                                <MenuItem Header="Delete" 
                                                          Visibility="{Binding Path=IsFolder, Converter={StaticResource convBoolYesNotVisibility}, ConverterParameter=NOT}"
                                                          Command="{Binding OnCommandFromView}" CommandParameter="MacroDelete" />
                                <!--#endregion-->
                            </ContextMenu>
                        </StackPanel.ContextMenu>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>
    </Grid>
</UserControl>
