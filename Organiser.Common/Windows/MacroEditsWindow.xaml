<Window x:Class="Organiser.Common.Windows.MacroEditsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Organiser.Common.Windows"
        mc:Ignorable="d"
        Title="Quick Edit" Height="600" Width="1000" Icon="/Organiser.Common;component/Image/browseo (1).ico">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Organiser.Common;component/GloableStyleDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!--<RichTextBox Margin="10" Name="rtb">
        </RichTextBox>-->
        <RichTextBox ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.VerticalScrollBarVisibility="Visible" Name="TextInput" AcceptsReturn="True" 
                     PreviewKeyDown="TextInput_PreviewKeyDown" TextChanged="TextChangedEventHandler">
            <RichTextBox.Resources>
                <Style TargetType="{x:Type Paragraph}">
                    <Setter Property="Margin" Value="1"/>
                </Style>
            </RichTextBox.Resources>
        </RichTextBox>

        <UniformGrid Grid.Row="1" Rows="1" Width="400">
            <Button Content="Save And Close" Name="btnSave" Click="btnSave_Click" Margin="5" Style="{StaticResource FlatbtnStyle}" />
            <Button Content="Close Without Saving" Name="btnclose" Click="btnclose_Click" Style="{StaticResource FlatbtnStyle}" />
        </UniformGrid>
    </Grid>
</Window>
