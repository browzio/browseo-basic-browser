<UserControl x:Class="Indexer.Controls.IndexerMainUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="500">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Organiser.Common;component/GloableStyleDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="WhiteSmoke">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Text="{Binding InputedLinks}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Verdana" FontSize="15" />
        
        <StackPanel Grid.Row="1" Margin="5">
            <CheckBox Content="Backlinks Indexer" IsChecked="{Binding BacklinksindexerChecked}" Margin="5,0,0,0" />
            <DockPanel Visibility="{Binding BacklinksindexerVisible}" Margin="5">
                <TextBlock Text="API Key"  Margin="5" VerticalAlignment="Center" />
                <TextBox Text="{Binding BacklinksindexerAPIKey}" Margin="5" />
            </DockPanel>

            <CheckBox Content="Link Indexer" IsChecked="{Binding LinkindexerChecked}" Margin="5,0,0,0" />
            <StackPanel Visibility="{Binding LinkindexerVisible}" Margin="5">
                <DockPanel>
                    <TextBlock Text="API Key" Width="63"  Margin="5" VerticalAlignment="Center" />
                    <TextBox Text="{Binding LinkindexerAPIKey}" Margin="5" />
                </DockPanel>
                <DockPanel>
                    <TextBlock Text="Campaign:" Width="63"  Margin="5" VerticalAlignment="Center" />
                    <TextBox Text="{Binding LinkIndexerCampaign}" Margin="5" />
                </DockPanel>
                <DockPanel>
                    <TextBlock Text="Drip Feed:" Width="63" Margin="5" VerticalAlignment="Center" />
                    <ComboBox Cursor="Hand" Background="#eeeeee" 
                          SelectedIndex="{Binding SIDripFeed}"
                          ItemsSource="{Binding DripFeeds}" />
                </DockPanel>
            </StackPanel>

            <CheckBox Content="Crazy Indexer" IsChecked="{Binding CrazyindexerChecked}" Margin="5,0,0,0" />
            <DockPanel Visibility="{Binding CrazyindexerVisible}" Margin="5">
                <TextBlock Text="API Key"  Margin="5" VerticalAlignment="Center" />
                <TextBox Text="{Binding CrazyindexerAPIKey}" Margin="5" />
            </DockPanel>

            <CheckBox Content="Instant Link Indexer" IsChecked="{Binding InstantlinkindexerChecked}" Margin="5,0,0,0" />
            <DockPanel Visibility="{Binding InstantlinkindexerVisible}" Margin="5">
                <TextBlock Text="API Key"  Margin="5" VerticalAlignment="Center" />
                <TextBox Text="{Binding InstantlinkindexerAPIKey}" Margin="5" />
            </DockPanel>
        </StackPanel>

        <Button Command="{Binding BtnOkClicked}" IsEnabled="{Binding BtnOkEnabled}" Grid.Row="2" Cursor="Hand" Style="{StaticResource FlatbtnStyle}">
            <TextBlock Text="Index It" HorizontalAlignment="Center" />
        </Button>

        <Grid Grid.Row="3" Visibility="{Binding ResponseVisible}">
            <TextBox Text="{Binding Response}" VerticalScrollBarVisibility="Auto" FontFamily="Verdana" FontSize="15" />
        </Grid>
    </Grid>
</UserControl>
