<UserControl x:Class="RssReader.Controlls.RssListsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controlls="clr-namespace:RssReader.Controlls"
             xmlns:helpers="clr-namespace:RssReader.Helpers"
             Name="uc"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/RssReader;component/Styles/ScrollBarStyle.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
            <helpers:booleanToVisible x:Key="boolToVis" />
        <helpers:ArithmaticConverter x:Key="arithConverter" />
        <DataTemplate x:Key="ItemTemplate2">
            <controlls:RssDisplayerControl Margin="10" Width="600"/>
        </DataTemplate>
            <ItemsPanelTemplate x:Key="ItemsPanelTemplate1">
                <StackPanel  Margin="0,0,0,0" 
                            ScrollViewer.CanContentScroll="True" 
                            VerticalAlignment="Top" />
        </ItemsPanelTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <ScrollViewer x:Name="MyScrollViewer" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding AllRssFeedsResults}"
                      x:Name="list_MyControls" HorizontalAlignment="Left"
                      Margin="0,8,0,0" VerticalAlignment="Top"
                      ItemTemplate="{StaticResource ItemTemplate2}"   
                      ItemsPanel="{StaticResource ItemsPanelTemplate1}" />
        </ScrollViewer>
    </Grid>
</UserControl>
