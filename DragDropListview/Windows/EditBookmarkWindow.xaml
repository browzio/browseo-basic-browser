<Window x:Class="DragDropListview.Windows.EditBookmarkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Width="350" Height="289" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <!-- Flat Button Style -->
        <Color x:Key="DefaultColor" A="255" R="240" G ="240" B="240"/>
        <Color x:Key="HighlightColor" A="255" R="229" G ="229" B="229"/>
        <Color x:Key="PressedColor" A="255" R="215" G ="215" B="215"/>

        <SolidColorBrush x:Key="DefaultBrush" Color="{StaticResource DefaultColor}" />
        <SolidColorBrush x:Key="HighlightBrush" Color="{StaticResource HighlightColor}" />
        <SolidColorBrush x:Key="PressedBrush" Color="{StaticResource PressedColor}" />

        <Style TargetType="Button" x:Key="FlatButtonStyle">
            <Setter Property="Background" Value="{StaticResource DefaultBrush}" />
            <Setter Property="BorderThickness" Value="0.6" />
            <Setter Property="BorderBrush" Value="DarkGray" />
            <Setter Property="Height" Value="22" />
            <Setter Property="Margin" Value="3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="0">
                            <ContentControl Content="{TemplateBinding Content}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HighlightBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource PressedBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- End Flat Button Style -->

        <Style TargetType="ComboBox" x:Key="FlatComboBoxStyle">
            <Setter Property="Background" Value="{StaticResource DefaultBrush}" />
            <Setter Property="BorderThickness" Value="0.6" />
            <Setter Property="BorderBrush" Value="DarkGray" />
            <Setter Property="Height" Value="22" />
            <Setter Property="Margin" Value="3" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HighlightBrush}" />
                </Trigger>
                <!--<Trigger Property="MouseDown" Value="True">
                    <Setter Property="Background" Value="{StaticResource PressedBrush}" />
                </Trigger>-->
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Name:" Height="18" Width="50" />
            <TextBox Name="tbName" Width="260" Height="25" />
        </StackPanel>

        <StackPanel Name="spUrl" Grid.Row="1" Orientation="Horizontal" Margin="10">
            <TextBlock Text="URL:" Height="18" Width="50" />
            <TextBox Name="tbURL" Width="260" Height="25" />
        </StackPanel>

        <StackPanel Name="spFolder" Grid.Row="2" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Folder:" Height="18" Width="50" />
            <ComboBox Name="cmbFolders" Width="260" Height="25" Style="{StaticResource FlatComboBoxStyle}" />
        </StackPanel>

        <CheckBox Grid.Row="3" Margin="10" Content="Save Time Stamp" Name="cbTimeStamt" x:FieldModifier="public" />
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="10" HorizontalAlignment="Right">
            <Button Name="btnCancel"  Width="70" Margin="10" Click="btnCancel_Click" Style="{StaticResource FlatButtonStyle}">
                <TextBlock HorizontalAlignment="Center" Text="Cancel" />
            </Button>

            <Button Name="btnSave" Width="70" Margin="10" Click="btnSave_Click" Style="{StaticResource FlatButtonStyle}">
                <TextBlock HorizontalAlignment="Center" Text="Save" />
            </Button>
        </StackPanel>
    </Grid>
</Window>
