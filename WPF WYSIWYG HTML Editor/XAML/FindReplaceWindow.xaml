<Window x:Class="WPF_WYSIWYG_HTML_Editor.XAML.FindReplaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find/Replace" Height="155" Width="320" ResizeMode="NoResize" Topmost="True" WindowStartupLocation="CenterScreen" Icon="/WYSIWYG;component/Images/refresh-icon.png">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Organiser.Common;component/GloableStyleDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Background="WhiteSmoke">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <DockPanel Margin="5">
            <TextBlock Text="Find:" Width="50" VerticalAlignment="Center" />
            <TextBox Name="tbFindText" Margin="5" />
        </DockPanel>
        
        <DockPanel Margin="5" Grid.Row="1">
            <TextBlock Text="Replace:" Width="50" VerticalAlignment="Center" />
            <TextBox Name="tbReplace" Margin="5" />
        </DockPanel>
        
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="5" HorizontalAlignment="Center">
            <Button Name="btnFind" Content="Find" Width="90" Margin="3" Click="btnFind_Click" Style="{StaticResource FlatbtnStyle}" />
            <Button Name="btnFR" Content="Find/Replace" Width="90" Margin="3" Click="btnFR_Click" Style="{StaticResource FlatbtnStyle}"  />
            <Button Name="btnReplaceA" Content="Replace All" Width="90" Margin="3" Click="btnReplaceA_Click" Style="{StaticResource FlatbtnStyle}"  />
        </StackPanel>
    </Grid>
</Window>
