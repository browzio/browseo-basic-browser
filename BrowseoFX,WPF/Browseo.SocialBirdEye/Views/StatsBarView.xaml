<UserControl x:Class="BrowseoFX_WPF.Browseo.SocialBirdEye.Views.StatsBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrowseoFX_WPF.Browseo.SocialBirdEye.Views"
             xmlns:converters="clr-namespace:Organiser.Common.Converters;assembly=Organiser.Common"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Organiser.Common;component/GloableStyleDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:NullToVisibleConverter x:Key="nullToVis" />
            <converters:DatetimeToStringConverter x:Key="DatetimeToStringConverter" />

            <ControlTemplate x:Key="lvNoScrollTemplate">
                <ItemsPresenter></ItemsPresenter>
            </ControlTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid Visibility="{Binding
                            RelativeSource={RelativeSource AncestorType={x:Type local:StatsBarView}},
                            Path=StatValue, 
                            Converter={StaticResource nullToVis}, 
                            ConverterParameter=NOT}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Image Margin="3"
               Width="28"
               Height="28"
               Source="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:StatsBarView}}, Path=StatImageUrl}" />

            <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:StatsBarView}}, Path=StatValue}"
                   
                   Grid.Column="1"
                   
                   FontSize="16"
                   
                   Margin="3"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>
