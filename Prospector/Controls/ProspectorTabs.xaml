<UserControl x:Class="Prospector.Controls.ProspectorTabs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Prospector.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="750">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Organiser.Common;component/GloableStyleDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border BorderThickness="0,1,0,0" BorderBrush="#d0d0d0">
        <TabControl Name="tbContrl" SelectedIndex="{Binding TCSelectedTabIndex}" Margin="0,10,0,0">
            <TabControl.Style>
                <Style TargetType="{x:Type TabControl}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TabControl}">
                                <Grid ClipToBounds="true" SnapsToDevicePixels="true" KeyboardNavigation.TabNavigation="Local">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition x:Name="ColumnDefinition0"/>
                                        <ColumnDefinition x:Name="ColumnDefinition1" Width="0"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition x:Name="RowDefinition0" Height="Auto"/>
                                        <RowDefinition x:Name="RowDefinition1" Height="*"/>
                                    </Grid.RowDefinitions>
                                    <ScrollViewer Grid.Column="0" Grid.Row="0" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Auto" Panel.ZIndex="3">
                                        <TabPanel x:Name="HeaderPanel" Grid.Column="0" IsItemsHost="true" Margin="0,0,0,-2" Grid.Row="0" KeyboardNavigation.TabIndex="1" />
                                    </ScrollViewer>
                                    <Border x:Name="ContentPanel" BorderThickness="0,1,0,0" Grid.Column="0" Panel.ZIndex="0"
                                        KeyboardNavigation.DirectionalNavigation="Contained" Grid.Row="1" KeyboardNavigation.TabIndex="2" KeyboardNavigation.TabNavigation="Local">
                                        <ContentPresenter Margin="0,15,0,0" x:Name="PART_SelectedContentHost" ContentSource="SelectedContent"  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        <!--<Grid Margin="0,15,0,0" x:Name="PART_SelectedContentHost"  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>-->
                                    </Border>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="TabStripPlacement" Value="Bottom">
                                        <Setter Property="Grid.Row" TargetName="HeaderPanel" Value="1"/>
                                        <Setter Property="Grid.Row" TargetName="ContentPanel" Value="0"/>
                                        <Setter Property="Height" TargetName="RowDefinition0" Value="*"/>
                                        <Setter Property="Height" TargetName="RowDefinition1" Value="Auto"/>
                                        <Setter Property="Margin" TargetName="HeaderPanel" Value="2,0,2,2"/>
                                    </Trigger>
                                    <Trigger Property="TabStripPlacement" Value="Left">
                                        <Setter Property="Grid.Row" TargetName="HeaderPanel" Value="0"/>
                                        <Setter Property="Grid.Row" TargetName="ContentPanel" Value="0"/>
                                        <Setter Property="Grid.Column" TargetName="HeaderPanel" Value="0"/>
                                        <Setter Property="Grid.Column" TargetName="ContentPanel" Value="1"/>
                                        <Setter Property="Width" TargetName="ColumnDefinition0" Value="Auto"/>
                                        <Setter Property="Width" TargetName="ColumnDefinition1" Value="*"/>
                                        <Setter Property="Height" TargetName="RowDefinition0" Value="*"/>
                                        <Setter Property="Height" TargetName="RowDefinition1" Value="0"/>
                                        <Setter Property="Margin" TargetName="HeaderPanel" Value="2,2,0,2"/>
                                    </Trigger>
                                    <Trigger Property="TabStripPlacement" Value="Right">
                                        <Setter Property="Grid.Row" TargetName="HeaderPanel" Value="0"/>
                                        <Setter Property="Grid.Row" TargetName="ContentPanel" Value="0"/>
                                        <Setter Property="Grid.Column" TargetName="HeaderPanel" Value="1"/>
                                        <Setter Property="Grid.Column" TargetName="ContentPanel" Value="0"/>
                                        <Setter Property="Width" TargetName="ColumnDefinition0" Value="*"/>
                                        <Setter Property="Width" TargetName="ColumnDefinition1" Value="Auto"/>
                                        <Setter Property="Height" TargetName="RowDefinition0" Value="*"/>
                                        <Setter Property="Height" TargetName="RowDefinition1" Value="0"/>
                                        <Setter Property="Margin" TargetName="HeaderPanel" Value="0,2,2,2"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="false">
                                        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Style>
            
            <TabItem Style="{StaticResource gmailTabStyle}" Header="Comment Backlinks">
                <controls:KeywordAndOptions />
            </TabItem>

            <TabItem Style="{StaticResource gmailTabStyle}"  Header="Forum">
                <controls:KeywordAndOptions />
            </TabItem>

            <TabItem Style="{StaticResource gmailTabStyle}"  Header="Guest Posts">
                <controls:KeywordAndOptions />
            </TabItem>

            <TabItem Style="{StaticResource gmailTabStyle}"  Header="Blog">
                <controls:KeywordAndOptions />
            </TabItem>

            <TabItem Style="{StaticResource gmailTabStyle}"  Header="Link Roundups">
                <controls:KeywordAndOptions />
            </TabItem>

            <TabItem Style="{StaticResource gmailTabStyle}"  Header="Custom">
                <controls:KeywordAndOptions />
            </TabItem>

            <TabItem Name="tabKingKontent" x:FieldModifier="public" Style="{StaticResource gmailTabStyle}"  Header="King Kontent">
                <controls:KeywordAndOptions />
            </TabItem>

            <TabItem Style="{StaticResource gmailTabStyle}"
                     Visibility="Collapsed"
                     Name="tabDW"
                     x:FieldModifier="public"
                     Header="Dark Web">
                <controls:KeywordOptionsDarkWeb />
            </TabItem>

            <TabItem Style="{StaticResource gmailTabStyle}"  Header="Saved">
                <controls:KeywordAndOptions />
            </TabItem>
        </TabControl>
    </Border>
</UserControl>
